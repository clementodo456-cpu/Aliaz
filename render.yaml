services:
  - type: worker
    name: photo-collage-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: MAX_IMAGES
        value: 12
      - key: SESSION_TIMEOUT
        value: 1800
